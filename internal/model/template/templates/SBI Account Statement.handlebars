{{#if (isDate ROW.A "D MMM YYYY")}}
  {{date ROW.A "D MMM YYYY"}} {{ROW.C}}
  {{#if (isBlank ROW.F)}}
    {{predictAccount prefix="Expenses"}}      {{amount ROW.E}} INR
    Assets:Checking
  {{else}}
    Assets:Checking                           {{amount ROW.F}} INR
    {{predictAccount prefix="Income"}}
  {{/if}}
{{/if}}
